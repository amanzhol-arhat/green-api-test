# Тестовое задание: Интерфейс взаимодействия с GREEN-API

Данный проект представляет собой одностраничное веб-приложение для тестирования базовых функций GREEN-API. Реализация выполнена на чистом JavaScript (Vanilla JS) без использования фреймворков, что обеспечивает высокую скорость работы и отсутствие лишних зависимостей.

## 📋 Функциональные возможности

Интерфейс поддерживает работу со следующими методами:
* **getSettings** — получение текущих настроек инстанса.
* **getStateInstance** — проверка статуса авторизации и готовности инстанса к работе.
* **sendMessage** — отправка текстовых уведомлений.
* **sendFileByUrl** — передача файлов через внешние URL-ссылки.

## 🛠 Технический стек

* **Frontend**: HTML5, CSS3 (Flexbox для верстки по макету).
* **Logic**: JavaScript (Fetch API, Async/Await).
* **API**: GREEN-API (Host: `7103.api.greenapi.com`).

## 🔧 Диагностика и работа с API

В рамках роли специалиста 2 линии поддержки, в проект заложены следующие особенности:
* **Логирование ответов**: Результат каждого запроса (включая ошибки) выводится в поле `Response` в формате JSON для детального анализа.
* **Валидация**: Реализована базовая проверка заполнения обязательных полей (`idInstance`, `apiTokenInstance`) перед отправкой запроса.
* **Автоматизация**: Скрипт автоматически извлекает имя файла из URL при использовании метода `sendFileByUrl`, минимизируя ввод данных пользователем.

## 🚀 Инструкция по запуску

1.  Клонируйте репозиторий:
    ```bash
    git clone [https://github.com/ВАШ_ЛОГИН/green-api-test.git](https://github.com/ВАШ_ЛОГИН/green-api-test.git)
    ```
2.  Откройте `index.html` в браузере.
3.  Введите данные вашего инстанса (ID и Token) из личного кабинета GREEN-API.
4.  Проверьте статус подключения кноп
